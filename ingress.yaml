apiVersion: networking.k8s.io/v1

kind: Ingress

metadata:

  name: aycap-prod-blue

  annotations:

    nginx.ingress.kubernetes.io/rewrite-target: /

spec:

  rules:

    - host: aycap.challenges.com

      http:

        paths:

          - path: /

            pathType: Prefix

            backend:

              service:

                name: aycap-prod-blue

                port:

                  number: 8000
